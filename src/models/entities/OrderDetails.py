class OrderDetails():
    
    def __init__(self, id, order_id, prodcut_id, quantity, unit_price, total_price):
        self.id = id
        self.order_id = order_id
        self.prodcut_id = prodcut_id
        self.quantity = quantity
        self.unit_price = unit_price 
        self.total_price = total_price
